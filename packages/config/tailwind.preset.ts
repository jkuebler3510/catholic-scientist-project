import type { Config } from 'tailwindcss';

const preset: Partial<Config> = {
  theme: {
    extend: {
      // Design tokens will be added here by 01-design-system
    },
  },
  plugins: [],
};

export default preset;
