# Sanity Studio

CMS for Society of Catholic Scientists.

## Getting started

```bash
pnpm install
pnpm --filter @catholic-scientists/studio dev
```

Studio will be available at `http://localhost:3333` (or a nearby port).

Configure `SANITY_STUDIO_PROJECT_ID` and `SANITY_STUDIO_DATASET` environment variables.
