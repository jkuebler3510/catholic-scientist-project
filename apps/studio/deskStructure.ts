// Export an empty function for now - Sanity will use the default structure.
// In production, this would configure the custom desk structure with
// singletons pinned, content grouped, etc. For v1, we use Sanity's default.
export const structure = () => undefined;
